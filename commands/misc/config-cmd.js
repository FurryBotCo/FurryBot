module.exports = (async(self,local)=>{
    /*if(local.args.length < 1) return new Error("ERR_INVALID_USAGE");
    switch(local.args[0]) {
        case "list":
            break;
        
        case "view":
        case "get":
            break;

        case "edit":
        case "set":
            break;

        case "reset":
            break;
    }*/
    // not in use yet
})